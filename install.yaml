- defaults:
    shell:
      stdout: true
      stderr: true

- shell:
    - bash -c 'if [ "$(uname)" == "Darwin" ]; then brew bundle --no-lock; fi'

- clean:
    ~/:
    ~/.config:
      recursive: true

- create:
    - ~/.config/gh

- link:
    ~/.bashrc:
    ~/.common.sh:
    ~/.ggitignore:
    ~/.ideavimrc:
    ~/.isort.cfg:
    ~/.zshrc:

- stow:
    config: ~/.config

- shell:
    - fish -c 'omf install'
