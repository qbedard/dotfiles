- defaults:
    shell:
      stdout: true
      stderr: true
- shell:
    - bash -c 'if [ "$(uname)" == "Darwin" ]; then brew bundle; fi'
    - bash -c 'macos-defaults apply macos-defaults.yaml'
    - bat cache --build
- clean:
    ~/:
    ~/.config:
      recursive: true
- create:
    - ~/.claude
    - ~/.config/fish/functions
    - ~/.config/gh
    - ~/.config/karabiner
    - ~/Library/Application Support/biome
    - ~/Library/Application Support/jesseduffield/lazydocker
    - ~/Library/Application Support/pypoetry
- link:
    ~/.amethyst.yml: amethyst.yaml
    ~/.bashrc:
    ~/.common.sh:
    ~/.hushlogin:
    ~/.npmrc:
    ~/.ripgreprc:
    ~/.sql-formatter.json:
    ~/.tflint.hcl:
    ~/.zshrc:
    ~/Library/Application Support/biome/biome.json: config/biome.json
    ~/Library/Application Support/jesseduffield/lazydocker/config.yml: lazydocker-config.yaml
    ~/Library/Application Support/pypoetry/config.toml: config/pypoetry/config.toml
- stow:
    claude: ~/.claude
    config: ~/.config
