[core]
	excludesFile = ~/.config/git/ignore
	pager = diff-so-fancy | $PAGER
[init]
	defaultBranch = main
[diff]
	tool = nvimdiff
[merge]
	tool = nvimdiff
[mergetool]
  prompt = true
[mergetool "nvimdiff"]
	path = nvim
	cmd = nvim -d $BASE $LOCAL $REMOTE $MERGED -c '$wincmd w' -c 'wincmd J'

; [pager]
; 	diff = delta
; 	log = delta
; 	reflog = delta
; 	show = delta

; [interactive]
; 	diffFilter = delta --color-only

; [delta]
; 	syntax-theme = gruvbox
[user]
	name = Quin Bedard
	email = tpbedard@gmail.com
