function layout() {
  return {
    name: "Wheel",
    initialState: {},
    commands: {},
    getFrameAssignments: (windows, screenFrame, state) => {},
    updateWithChange: (change, state) => {},
  };
}
