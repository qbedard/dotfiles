function _layout() {
  return {
    name: "Wheel",
    initialState: {},
    commands: {},
    getFrameAssignments: (_windows, _screenFrame, _state) => {},
    updateWithChange: (_change, _state) => {},
  };
}
